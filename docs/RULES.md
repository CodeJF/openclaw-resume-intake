# RULES.md

这个文件保持精简。

- PDF 简历录入的默认处理，直接走 `skills/resume-intake-workflow/SKILL.md`
- 详细业务规则不要重复写在这里，避免消耗 token
- 需要目标配置时，读取 `config/bitable-targets.json`
- 需要字段、顺序、护栏时，读取 skill 自带的 `references/` 或运行 `scripts/`
